<UserControl x:Class="PharmaDesk.Views.AuditLogView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml">
    <Border Style="{StaticResource Card}">
        <DockPanel><TextBlock DockPanel.Dock="Top" Text="Audit log" FontSize="28" FontWeight="Bold" />
            <DataGrid ItemsSource="{Binding Logs}" AutoGenerateColumns="False" IsReadOnly="True">
                <DataGrid.Columns><DataGridTextColumn Header="Data" Binding="{Binding Timestamp}" Width="170" /><DataGridTextColumn Header="User" Binding="{Binding User.Username}" Width="140" /><DataGridTextColumn Header="Actiune" Binding="{Binding Action}" Width="220" /><DataGridTextColumn Header="Tabel" Binding="{Binding TableName}" Width="160" /><DataGridTextColumn Header="Record" Binding="{Binding RecordId}" Width="100" /></DataGrid.Columns>
            </DataGrid>
        </DockPanel>
    </Border>
</UserControl>
